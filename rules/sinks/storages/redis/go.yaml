sinks:

  - id: Storages.Redis.Read
    name: Redis DB(Read)
    domains:
      - redis.io
    patterns:
      - "(?i).*(cache).*(Get|HGet|HGetAll).*"
    tags:

  - id: Storages.Redis.Write
    name: Redis DB(Write)
    domains:
      - redis.io
    patterns:
      - "(?i).*(cache).*(Set|HSet|Del).*"
    tags:
